name: My Workflow
on:
  pull:
    branches: [ master ]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
    - name: My Step
      env:
        MY_SECRET: ${{ secrets.MY_SECRET }}
      run: echo $MY_SECRET
